[web]
${node1_id}

[ctr]
${node2_id} 

[mon]
${node3_id}

[nodes:children]
web
mon
ctr

[nodes:vars]
ansible_user=ubuntu
ansible_connection=aws_ssm
ansible_aws_ssm_bucket_name=${bucket_id}
ansible_aws_ssm_region=${region}